<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- droid-mcp-tls generates a self-signed certificate for the HTTP
         transport. No permissions required: the keystore lives in the host
         app's private storage. -->
</manifest>
