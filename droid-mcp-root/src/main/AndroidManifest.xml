<!--
    Intentionally empty. The Tier 5 root backend doesn't declare any Android
    permissions — libsu's privilege comes from the `su` shell at runtime,
    gated by the user's superuser manager (Magisk / KernelSU / SuperSU).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
