package io.droidmcp.core

/**
 * The droid-mcp SDK version, reported as the server version in the MCP `initialize`
 * handshake and broadcast over mDNS. Keep in sync with the Gradle `VERSION_NAME`.
 */
const val DROID_MCP_VERSION: String = "0.10.1"
